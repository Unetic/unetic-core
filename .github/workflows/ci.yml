name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

permissions:
  contents: read

jobs:
  test:
    uses: Unetic/packages/.github/workflows/reusable-rust.yml@main
    with:
      component_name: unetic-core
      binary_name: unetic-core
      release: false
