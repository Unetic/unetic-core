name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing semantic tag to publish, e.g. v0.1.0
        required: true
        type: string

concurrency:
  group: unetic-core-release-${{ inputs.tag || github.event.release.tag_name || github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    uses: Unetic/packages/.github/workflows/reusable-rust.yml@main
    with:
      component_name: unetic-core
      binary_name: unetic-core
      release: true
      ref: ${{ inputs.tag || github.event.release.tag_name || github.ref_name }}
